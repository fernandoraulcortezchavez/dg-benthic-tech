name: Benthic Frontend Redeployment

on: 
  push:
    branches: [main]
    paths:
      - 'benthic-website/**'
  workflow_dispatch:

jobs:
  # ---------------------------------------------
  # REDEPLOY NEW VERSION OF WEBPAGE
  # ---------------------------------------------
    redeploy-frontend:
      runs-on: benthic-hosted

      defaults:
        run:
          working-directory: ./benthic-website

      steps:
          - name: Checkout code
            uses: actions/checkout@v3

          - name: Install and Build
            run: |
              npm ci
              npm run build
            
          - name: Copy Build Files Nginx Directory
            run: |
              # Clear old files
              rm -rv /var/www/benthic-technologies/*
              # Copy new build files
              cp dist/* /var/www/react-app/



